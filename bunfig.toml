[test]
# Threshold applies to aggregate totals across all files.
# daemon.ts is excluded from meaningful coverage (requires the native binary),
# and rethocker.ts has subprocess-only functions that can't be unit tested.
coverageThreshold = { lines = 0.9 }
